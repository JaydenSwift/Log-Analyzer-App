<Window x:Class="Log_Analyzer_App.ExportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:Log_Analyzer_App"
        mc:Ignorable="d"
        Title="Export Filtered Log" 
        Height="550" Width="400" 
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="{x:Null}"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <materialDesign:Card UniformCornerRadius="15" Margin="10" Padding="20" Background="{DynamicResource MaterialDesignPaper}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Grid.Row="0" Text="Export Filtered Log" FontSize="24" FontWeight="Bold" Margin="0 0 0 15" Foreground="{DynamicResource PrimaryHueMidBrush}"/>

            <!-- Export Format Selection -->
            <StackPanel Grid.Row="1" Margin="0 0 0 15">
                <TextBlock Text="1. Select Export Format" FontWeight="SemiBold" Opacity="0.8" Margin="0 0 0 5"/>
                <ComboBox x:Name="FormatComboBox"
                          materialDesign:HintAssist.Hint="Format"
                          SelectedItem="{Binding SelectedFormat, Mode=TwoWay}"
                          Margin="0 0 0 10">
                    <ComboBoxItem Content="CSV (Comma Separated Values)"/>
                    <ComboBoxItem Content="TXT (Plain Text - Tab Separated)"/>
                    <ComboBoxItem Content="JSON (JavaScript Object Notation)"/>
                </ComboBox>

                <TextBlock Text="2. Columns to Export" FontWeight="SemiBold" Opacity="0.8" Margin="0 5 0 5"/>
                <CheckBox Content="Select/Deselect All" Checked="SelectAll_Checked" Unchecked="SelectAll_Unchecked" Margin="0 0 0 5"/>
            </StackPanel>

            <!-- Column Selection List -->
            <materialDesign:Card Grid.Row="2" UniformCornerRadius="8" Padding="15" Margin="0 0 0 15" Background="{DynamicResource MaterialDesignLightBackground}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <!-- ColumnControls from the LogAnalyzerView are passed in and bound here -->
                    <ItemsControl ItemsSource="{Binding ColumnControls}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding Header}" 
                                          IsChecked="{Binding IsVisible, Mode=TwoWay}"
                                          Margin="0 5 0 5"
                                          Foreground="{DynamicResource MaterialDesignBody}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </materialDesign:Card>

            <!-- Action Buttons -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="CANCEL" 
                        Style="{StaticResource MaterialDesignFlatButton}" 
                        Margin="0 0 10 0"
                        Click="Cancel_Click"/>

                <Button Content="EXPORT" 
                        x:Name="ExportButton"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        materialDesign:ButtonAssist.CornerRadius="5"
                        Background="{DynamicResource SecondaryHueMidBrush}"
                        Click="Export_Click"/>
            </StackPanel>
        </Grid>
    </materialDesign:Card>
</Window>